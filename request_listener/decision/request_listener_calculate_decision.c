#include "request_listener_calculate_decision.h"

uint8_t request_listener_calculate_decision(char *req, size_t reqlen, char *res, size_t *reslen) {
  printf("calculating decision...\n");

  return REQLIST_OK;
}